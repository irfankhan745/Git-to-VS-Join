# Git-to-VS-Join
<p>
how to add git Repo in VS Code with READ>ME file 
<p>
1. Create a repo with README.md 
<p>
3. open terminal try to check Git is werking on not using(git --version)
<p>
4. write (git clone) in terminal 
<p>
5. copy HTTPS link from GitHub and peste it in Vs terminal 
<p>
6. write (cd 'folder name') 
<p>
7. now you are in the new repo witch you Upload
<p>
8. Ctrl+s for save changes what you do in file
<p>
9. you can check satatus using 'git status'
<p> 
    9.1 using this you can check which file is changed and add new
<p>
10. for save all changes type 'git add file name' if you create multiple file so you can save all file in one click using this 'git add .'    
<p>
11. commit your changes its mean give the name of your new changes for shoing in the git 
   <p>
   11.1 you can make a commit using this = git commit -m "commit message"
<p>
12. check using 'git status' about changes it's complite or not if changes updated so in show a messes "nothing is chagne, no commit'
<p>
13. and then last step upload changes in GitHub 'git push origin main'.   
   <p>
   13.1 so now your changes succesfully updated on GitHub.